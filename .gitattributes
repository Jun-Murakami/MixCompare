# 改行コードは LF に統一する（Windows / Linux / macOS 間の CRLF 差分を防ぐ）。
#   text=auto : テキストと判定したファイルをリポジトリ内では LF に正規化
#   eol=lf    : チェックアウト時も常に LF（OS やローカル設定に依存しない＝LF強制）
* text=auto eol=lf

# サブモジュールは独立した git リポジトリなので、親側の改行正規化（renormalize）の
# 対象から完全に除外する。これを怠ると extras/Build 等の削除や全ファイルの CRLF↔LF
# 書き換えが発生し、JUCE のビルドが壊れる（JUCEModuleSupport.cmake が見つからない等）。
JUCE/**                   -text
clap-juce-extensions/**   -text

# パッチファイルはバイト保全（-text）。JUCE のソースは CRLF なので、パッチの
# コンテキスト行も CRLF を含む。text=auto eol=lf の正規化で LF 化されると
# git apply がコンテキスト不一致で失敗し、ApplyJucePatch が「パッチ無し」の
# バイナリを黙って出荷する事故になる（v3.0.9-beta で childlog パッチ欠落が発生）。
patches/*.patch           -text

# バイナリは変換しない（現在追跡中の実バイナリは画像・アイコンのみ）
*.png   binary
*.ico   binary
*.icns  binary
*.jpg   binary
*.jpeg  binary
*.gif   binary

# 将来追加されうるバイナリ（フォント・音声・アーカイブ・ビルド成果物）
*.woff  binary
*.woff2 binary
*.ttf   binary
*.otf   binary
*.wav   binary
*.aiff  binary
*.flac  binary
*.zip   binary
*.so    binary
*.dll   binary
*.dylib binary
*.a     binary
*.wasm  binary
*.vst3  binary
*.clap  binary
